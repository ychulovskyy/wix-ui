import { BaseDriver, DriverFactory } from './../../common/BaseDriver.protractor';
export interface TimePickerDriver extends BaseDriver {
}
export declare const timePickerDriverFactory: DriverFactory<TimePickerDriver>;

import { BaseDriver, DriverFactory } from './../../common/BaseDriver.protractor';
export interface VBoxDriver extends BaseDriver {
}
export declare const vBoxDriverFactory: DriverFactory<VBoxDriver>;
